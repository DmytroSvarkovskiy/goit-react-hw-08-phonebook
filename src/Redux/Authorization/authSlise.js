import { createSlice } from '@reduxjs/toolkit';

const authSlise = createSlice({
    name: 'auth',
    initialState: {
        items: [],
        isLoading: false,
        error: null
    }, extraReducers:


})